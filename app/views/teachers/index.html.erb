<h3>Professores do Cursinho</h3>

<table>
    <tbody>
        <tr>
            <th>Nome</th>
            <th>Endereço</th>
            <th>Disciplina</th>
        </tr>
        <% @teachers.each do |teacher|  %>
            <tr>
                <td><%= teacher.name %></td>
                <td><%= teacher.address %></td>
                <td><%= Subject.find(teacher.disciplina).name %></td>
                <td><%= link_to 'Visualizar', teacher %></td>
                <td><%= link_to "Editar", edit_teacher_url(teacher) %></td>
                <td><%= link_to "Deletar", teacher, method: :delete, data: {confirm: "Tem certeza?"} %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= link_to 'Novo Professor', new_teacher_path %>